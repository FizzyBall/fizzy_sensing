In this data folder the CNN and RandomForest methods are split because the data after processing is different
See the excel sheet for a short description about wat happens in a certain dataset. 
The subfolders speak for itself. Training data was gathered from the BEP group, the testing data from CoR employees.
